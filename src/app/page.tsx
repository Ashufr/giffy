import React from 'react';
import Collage from '@/components/Collage';

export default function App() {
  return (
    <div style={{ padding: '2rem', background: 'linear-gradient(to bottom, #f3f4f6, #fff)', minHeight: '100vh' }}>
      <Collage />
    </div>
  );
}
